/**
initUser();

if(!Authentic(userinfo))
  printf("username or password error!");
 
close login
switch(userinfo.type)
   = user
   = doctor
   ...
*/

////////////////////Logic module function//////////////////
/**
 * initUser();
 * Authentic(userinfo)
 * 
 * AddUser(userinfo) // check if user exist
 * UpdateUser(userinfo)
 * DelUser()
 */

////////////////////data module function//////////////////
/**
 * ReadUser(filename)
 * Saveuser(filename)
 * 
 * FindUserinFile(userinfo)
 * 
 */


lanuch.json
  "preLaunchTask" = tasks.label
  "program" = build/hho_test.exe

git≥£”√√¸¡Ó
git add .
git commit -m "   "
git push

git init
git clone --depth=1 https://github.com/jryx0/MyTest.git
git remote add origin https://github.com/jryx0/MyTest.git
git push -u origin master

---------------------------------------------------------------------------------
git init //?????git????? 
git add README.md //?????????????? 
git commit -m ?first commit? //????????README.md ????????????? 
git remote add Ceres your_first_git_address //????git address???Ceres 
git push -u Ceres master //???????????????????????push???????
 
 
//??????????????README?????????? 
//??????????????????????????????commit?? 
//??????????????????????????
 
git remote add Mars your_second_git_address //????git address???Mars 
git push -u Mars master //???????????master??